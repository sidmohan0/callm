streamlit>=1.22.0
numpy>=1.20.0
matplotlib>=3.5.0
python-dotenv>=0.19.0

# Dependencies for local model (replacing OpenAI)
torch>=2.0.0
transformers>=4.30.0
accelerators>=0.1.0  # Optional: for faster inference if available
